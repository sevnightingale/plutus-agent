"""Calendars integration — curated event-timing data points (earnings, lock-ups).

The desk's machine-readable freshness caps: NVDA earnings windows and the
SPCX lock-up unlock stages. Curated tables with source+verified metadata
(see data_points.py for the maintenance discipline).
"""
from . import data_points
