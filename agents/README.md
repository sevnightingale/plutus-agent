# agents/ — the trading desk roster

One directory per desk agent, each holding an `AGENT.md` context recipe read
directly by the spawn mechanism. Populated during the rebuild — the roster and
format are specified in `rebuild-architecture.md` §2 and §19.
